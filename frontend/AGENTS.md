# Frontend Agent Rules

- Preserve the existing Vue/Vite/Tailwind stack and current admin-console patterns.
- Run the minimum relevant frontend checks before claiming completion.
- When a task affects critical UI flows, make sure the UAT evidence path is updated or referenced.
- Do not introduce new frontend infrastructure unless the task explicitly requires it.
